# run: conda env create --file environment.yml
name: binder-coda-environment
channels:
  - conda-forge
dependencies:
  - cmake
  - numpy
  - scipy
  - jupyterlab_code_formatter
  - pip:
    - nbgitpuller
    - black
    - isort
